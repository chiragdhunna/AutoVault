/**
 * Side panel entry component.
 *
 * Placeholder for now — the copy-field UI, profile switcher, search and
 * documents dropdown are added in the following commits. Kept intentionally
 * minimal so this commit only wires up the new panel surface.
 */
export function Sidepanel() {
  return (
    <div class="sp">
      <header class="sp-header">
        <span class="sp-logo">🔐</span>
        <span class="sp-name">AutoVault</span>
      </header>
    </div>
  );
}
